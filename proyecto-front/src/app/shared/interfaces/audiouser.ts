export interface Audiouser {
    microphone: boolean, //si esta en 1, significa que si estamos mandando voz
    mute :boolean, //si mute esta en 1, es porque no queremos escuchar 
    username: string,
    userId: string
}
